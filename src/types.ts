export interface ModuleType {
  title: string;
  description: string;
  points: string[];
}

export interface BonusType {
  title: string;
  value: string;
  description: string;
  benefits: string[];
}
